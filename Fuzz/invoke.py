debug = True

run = {"echo": True}

# run-time is collection of settings defines how every command should be executed via CommandExecutor
run_time = {
    # should crated script be executed or only created
    "execute_created_script": True,
}
